integrations:
  certManager:
    enabled: true
    issuer:
      kind: ClusterIssuer
      name: my-issuer
  externalDNS:
    enabled: true
    targetService:
      name: kube-dns
      namespace: kube-system
external-dns:
  enabled: true
cert-manager:
  enabled: true
